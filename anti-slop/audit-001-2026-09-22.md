# Antislop Audit Report #001

- **Project**: SpillSense Maritime C2 (SIH 26143)
- **Date**: 2026-09-22
- **Mode**: Mode 2 (After Project Transformation Audit)
- **Scope**: macOS Neumorphic Desktop UI Shell, 3-Tier Layout, SAR Detection Lab, and Executive C2 Dashboard

---

## Findings List

| # | Priority | Rule | File / Component | One-Line Reason |
|---|----------|------|-------------------|-----------------|
| **1** | **HIGH** | **R-02 (Copywriting)** | `frontend/src/components/views/DetectionView.tsx:36`, `frontend/src/components/LeafletMap.tsx:301, 482` | Em dash character (`—`) used in GeoTIFF status string and map popup headers instead of colon or parentheses. |
| **2** | **HIGH** | **R-26 (Interactive Elements)** | `frontend/src/components/Sidebar.tsx:173, 240` | "EEZ Archive" and "Off-Grid / Unattributed" folder rows appear clickable but lack click handlers or destination filters. |
| **3** | **HIGH** | **R-26 (Interactive Elements)** | `frontend/src/components/Topbar.tsx:154` | Notifications alert icon button has an unread count badge but lacks an `onClick` handler or notification drawer toggle. |
| **4** | **HIGH** | **R-32 (Keyboard Accessibility)** | `frontend/src/components/Sidebar.tsx`, `frontend/src/components/views/DashboardView.tsx` | Metric cards and incident folder items are `div` elements without `tabIndex`, keyboard activation (`Enter`/`Space`), or `:focus-visible` outlines. |
| **5** | **MEDIUM** | **R-10 (Glassmorphism)** | `frontend/src/index.css` | Backdrop-filter blur is applied simultaneously on drawer, topbar, metric cards, and floating controls; metric cards should use solid frosted fill to avoid blur-stacking. |
| **6** | **MEDIUM** | **R-08 (Button Arrows)** | `frontend/src/components/views/DashboardView.tsx` | All 4 metric card footers include `arrow_forward` icons by default; only external navigation cards should carry directional cues. |
| **7** | **LOW** | **R-25 (Color Contrast)** | `frontend/src/index.css:dock-vertical-label` | Vertical engraved brand label on the dock uses `rgba(255, 255, 255, 0.3)` on `#16171B`, measuring below WCAG AA 3:1 contrast. |

---

## Instructions for Next Step
Per Antislop Mode 2 rules:
1. Please review this numbered findings list.
2. Select which numbers you would like me to fix (e.g. "Fix 1, 2, 3, 4" or "Fix all").
3. Items not selected will remain untouched.
